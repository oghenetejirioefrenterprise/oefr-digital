# Moved → `~/apps/cycle-trader`

This project relocated to **`/home/oghenetejiri/apps/cycle-trader`** on 2026-07-25
(owner instruction) so it sits alongside the other `~/apps/<project>` apps and
gives M2's Vercel deploy a clean project root.

**The copies of `SPEC.md`, `PRD.md`, `CLAUDE.md`, `docs/` and `data/` still in this
directory are STALE.** Do not read, edit, or implement against them. `SPEC.md` is
normative for a live trading system, and two live copies of a normative spec is
the exact drift hazard this project already hit once with `CY-1-FROZEN-SPEC.md`
(see `SPEC.md` §13 provenance block at the canonical location).

Retiring this directory entirely is pending owner sign-off.
