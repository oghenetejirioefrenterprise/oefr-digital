# Session Log — 2026-04-27

All Telegram exchanges for the dream cycle to process.
The dream cycle extracts decisions, issues, tasks, and insights
into the wiki knowledge base for long-term storage.

### [08:49] blockers
**cciephantom:** Reddit did not accept API request registration

**Trinity:** What exactly did Reddit say? Need the error message to triage — common rejection reasons differ wildly:

- **"You broke Reddit"** / generic 500 → Reddit's app-create page is just buggy; works on retry or a different browser
- **"Username/email not verified"** → account needs verified email + a few days of age
- **"Invalid redirect URI"** → Reddit's gotten strict; `http://localhost:8080/` (with trailing slash) sometimes works when `http://localhost:8080` doesn't
- **App name collision** → `oefr-clawdbot` may already exist; try `oefr-trinity-bot` or any unique name
- **Silently disappears after submit** → uBlock/privacy extensions kill the form; try incognito

Two fallback paths I can pursue in parallel so we don't stay wedged:

**Fallback A — browser auth-code flow (still needs app registration but tolerates more setups):** If you can get ANY app type registered (not just `script` — `web app` works too with redirect `http://localhost:8080`), I can use `node skills/reddit/scripts/reddit.mjs login` which does the OAuth browser dance and persists a refresh token. Refresh tokens never expire, so it's a one-time auth.

**Fallback B — pure browser automation (no API, no app registration needed):** Drop the OAuth wedge entirely. Use the existing CDP browser session to post to Reddit the same way we post to Facebook/Pinterest/X. ~30 min to write a `reddit-post-cdp.py` script following the Facebook ship pattern. This was the cargo-culted approach Apr 26 self-improvement said wasn't needed — but if Reddit refuses to register apps, browser-first is back on the table by necessity.

**Recommendation:** Send me the exact error/screenshot, I'll diagnose. Meanwhile I'll start Fallback B in the background — even if API works later, having a CDP-based Reddit poster is a useful backup (Reddit can revoke API access at any time, as the 2023 API drama showed).

One more thing worth checking: is the Reddit account `2FA-enabled`? If yes, password-grant OAuth is dead even with credentials (p
