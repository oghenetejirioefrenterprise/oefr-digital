# CLAUDE.md — STALE TREE, DO NOT WORK HERE

**This directory is superseded. The live project is `/home/oghenetejiri/apps/cycle-trader`.**

If your working directory is here, change to `/home/oghenetejiri/apps/cycle-trader`
before doing anything. Read that directory's `CLAUDE.md` for the real project
instructions — it is the canonical one.

The `SPEC.md`, `PRD.md`, `docs/` and `data/` files still sitting in this directory
are **stale copies** left behind by the 2026-07-25 relocation. `SPEC.md` is
normative for a live trading system, so implementing against the copy here would
be implementing against the wrong rules. This project has already been bitten once
by exactly this hazard — see the provenance block in the canonical `SPEC.md` §13,
which documents `CY-1-FROZEN-SPEC.md` drifting from its restatement.

Full retirement of this directory is pending owner sign-off; until then this file
exists to stop agents and readers landing here by accident.
