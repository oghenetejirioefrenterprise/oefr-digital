# DataStructured — Python runtime dependencies
# Pinned to the versions verified in the shared oefr venv (Python 3.12).
# Install:  pip install -r requirements.txt  &&  playwright install chromium

# Payments
stripe==15.0.0

# Data harvesting / cleaning
pandas==2.2.2
requests==2.32.5
beautifulsoup4==4.14.3
lxml==6.0.2
openpyxl==3.1.5

# Validation
jsonschema==4.23.0

# Browser automation (social distribution, Gumroad publishing)
playwright==1.58.0
browser-use==0.12.6

# LLM clients (NVIDIA NIM via OpenAI-compatible API, etc.)
openai==2.16.0

# Config
python-dotenv==1.2.1

# --- dev / test only ---
pytest==9.0.2
